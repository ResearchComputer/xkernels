{
  "id": "per_token_group_quant_fp8@1.0.0",
  "name": "per-token-group fp8 e4m3 quantization",
  "version": "1.0.0",
  "kernel": "per_token_group_quant_fp8",
  "op": {
    "signature": "(q[G,B] fp8, scale[G] fp32) = quantize(x[G,B]) per group; amax=reduce_max|x|; scale=amax/FP8_MAX; q=clamp(x/scale).fp8",
    "canonical_op": "reduce",
    "fusions": []
  },
  "inputs": {
    "x": {
      "dtype": [
        "fp32",
        "bf16"
      ],
      "rank": 2,
      "layout": "row_major",
      "shape_symbols": [
        "G",
        "B"
      ]
    }
  },
  "outputs": {
    "q": {
      "dtype": [
        "fp8"
      ],
      "rank": 2,
      "layout": "row_major",
      "shape_symbols": [
        "G",
        "B"
      ]
    },
    "scale": {
      "dtype": [
        "fp32"
      ],
      "rank": 1,
      "layout": "row_major",
      "shape_symbols": [
        "G"
      ]
    }
  },
  "constraints": [
    "B % 16 == 0"
  ],
  "preconditions": [
    "x is the GROUPED view [G, B] of an activation (G = num tokens * K//block);",
    "caller reshapes [M,K] -> [M*(K//block), block] and the outputs back",
    "fp8 dtype is float8_e4m3fn (FP8_MAX=448); fnuz (240) is an arch override"
  ],
  "numerics": {
    "reference": "xkernels.vkl.auto:per_token_group_quant_fp8",
    "rtol": 0.01,
    "atol": 0.01,
    "reduce_dtype": "fp32",
    "cross_backend_rtol": 0.01,
    "by_dtype": {
      "fp32": {
        "rtol": 1e-06,
        "atol": 1e-06
      },
      "bf16": {
        "rtol": 0.01,
        "atol": 0.01
      }
    },
    "notes": "Per-token-group fp8 quant. amax reduced in fp32; scale = amax/FP8_MAX (OCP e4m3fn, max 448). Bit-exact with the hand ops/gemm/reference.py:per_token_group_quant_fp8 (same op order). bf16 rtol covers the amax reduction order on GPU."
  },
  "shape_sweep": "per_token_group_quant_fp8",
  "composes_with": []
}
